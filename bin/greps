#!/usr/bin/perl
#line 2 "/usr/bin/par-archive"
eval 'exec /usr/bin/perl  -S $0 ${1+"$@"}'
    if 0; # not running under some shell

package __par_pl;

# --- This script must not use any modules at compile time ---
# use strict;

#line 158

my ($par_temp, $progname, @tmpfile);
END { if ($ENV{PAR_CLEAN}) {
    require File::Temp;
    require File::Basename;
    require File::Spec;
    my $topdir = File::Basename::dirname($par_temp);
    outs(qq{Removing files in "$par_temp"});
    File::Find::finddepth(sub { ( -d ) ? rmdir : unlink }, $par_temp);
    rmdir $par_temp;
    # Don't remove topdir because this causes a race with other apps
    # that are trying to start.

    if (-d $par_temp && $^O ne 'MSWin32') {
        # Something went wrong unlinking the temporary directory.  This
        # typically happens on platforms that disallow unlinking shared
        # libraries and executables that are in use. Unlink with a background
        # shell command so the files are no longer in use by this process.
        # Don't do anything on Windows because our parent process will
        # take care of cleaning things up.

        my $tmp = new File::Temp(
            TEMPLATE => 'tmpXXXXX',
            DIR => File::Basename::dirname($topdir),
            SUFFIX => '.cmd',
            UNLINK => 0,
        );

        print $tmp "#!/bin/sh
x=1; while [ \$x -lt 10 ]; do
   rm -rf '$par_temp'
   if [ \! -d '$par_temp' ]; then
       break
   fi
   sleep 1
   x=`expr \$x + 1`
done
rm '" . $tmp->filename . "'
";
            chmod 0700,$tmp->filename;
        my $cmd = $tmp->filename . ' >/dev/null 2>&1 &';
        close $tmp;
        system($cmd);
        outs(qq(Spawned background process to perform cleanup: )
             . $tmp->filename);
    }
} }

BEGIN {
    Internals::PAR::BOOT() if defined &Internals::PAR::BOOT;

    eval {

_par_init_env();

my $quiet = !$ENV{PAR_DEBUG};

# fix $progname if invoked from PATH
my %Config = (
    path_sep    => ($^O =~ /^MSWin/ ? ';' : ':'),
    _exe        => ($^O =~ /^(?:MSWin|OS2|cygwin)/ ? '.exe' : ''),
    _delim      => ($^O =~ /^MSWin|OS2/ ? '\\' : '/'),
);

_set_progname();
_set_par_temp();

# Magic string checking and extracting bundled modules {{{
my ($start_pos, $data_pos);
{
    local $SIG{__WARN__} = sub {};

    # Check file type, get start of data section {{{
    open _FH, '<', $progname or last;
    binmode(_FH);

    # Search for the "\nPAR.pm\n signature backward from the end of the file
    my $buf;
    my $size = -s $progname;
    my $offset = 512;
    my $idx = -1;
    while (1)
    {
        $offset = $size if $offset > $size;
        seek _FH, -$offset, 2 or die qq[seek failed on "$progname": $!];
        my $nread = read _FH, $buf, $offset;
        die qq[read failed on "$progname": $!] unless $nread == $offset;
        $idx = rindex($buf, "\nPAR.pm\n");
        last if $idx >= 0 || $offset == $size || $offset > 128 * 1024;
        $offset *= 2;
    }
    last unless $idx >= 0;

    # Seek 4 bytes backward from the signature to get the offset of the 
    # first embedded FILE, then seek to it
    $offset -= $idx - 4;
    seek _FH, -$offset, 2;
    read _FH, $buf, 4;
    seek _FH, -$offset - unpack("N", $buf), 2;
    read _FH, $buf, 4;

    $data_pos = (tell _FH) - 4;
    # }}}

    # Extracting each file into memory {{{
    my %require_list;
    while ($buf eq "FILE") {
        read _FH, $buf, 4;
        read _FH, $buf, unpack("N", $buf);

        my $fullname = $buf;
        outs(qq(Unpacking file "$fullname"...));
        my $crc = ( $fullname =~ s|^([a-f\d]{8})/|| ) ? $1 : undef;
        my ($basename, $ext) = ($buf =~ m|(?:.*/)?(.*)(\..*)|);

        read _FH, $buf, 4;
        read _FH, $buf, unpack("N", $buf);

        if (defined($ext) and $ext !~ /\.(?:pm|pl|ix|al)$/i) {
            my $filename = _tempfile("$crc$ext", $buf, 0755);
            $PAR::Heavy::FullCache{$fullname} = $filename;
            $PAR::Heavy::FullCache{$filename} = $fullname;
        }
        elsif ( $fullname =~ m|^/?shlib/| and defined $ENV{PAR_TEMP} ) {
            my $filename = _tempfile("$basename$ext", $buf, 0755);
            outs("SHLIB: $filename\n");
        }
        else {
            $require_list{$fullname} =
            $PAR::Heavy::ModuleCache{$fullname} = {
                buf => $buf,
                crc => $crc,
                name => $fullname,
            };
        }
        read _FH, $buf, 4;
    }
    # }}}

    local @INC = (sub {
        my ($self, $module) = @_;

        return if ref $module or !$module;

        my $filename = delete $require_list{$module} || do {
            my $key;
            foreach (keys %require_list) {
                next unless /\Q$module\E$/;
                $key = $_; last;
            }
            delete $require_list{$key} if defined($key);
        } or return;

        $INC{$module} = "/loader/$filename/$module";

        if ($ENV{PAR_CLEAN} and defined(&IO::File::new)) {
            my $fh = IO::File->new_tmpfile or die $!;
            binmode($fh);
            print $fh $filename->{buf};
            seek($fh, 0, 0);
            return $fh;
        }
        else {
            my $filename = _tempfile("$filename->{crc}.pm", $filename->{buf});

            open my $fh, '<', $filename or die "can't read $filename: $!";
            binmode($fh);
            return $fh;
        }

        die "Bootstrapping failed: cannot find $module!\n";
    }, @INC);

    # Now load all bundled files {{{

    # initialize shared object processing
    require XSLoader;
    require PAR::Heavy;
    require Carp::Heavy;
    require Exporter::Heavy;
    PAR::Heavy::_init_dynaloader();

    # now let's try getting helper modules from within
    require IO::File;

    # load rest of the group in
    while (my $filename = (sort keys %require_list)[0]) {
        #local $INC{'Cwd.pm'} = __FILE__ if $^O ne 'MSWin32';
        unless ($INC{$filename} or $filename =~ /BSDPAN/) {
            # require modules, do other executable files
            if ($filename =~ /\.pmc?$/i) {
                require $filename;
            }
            else {
                # Skip ActiveState's sitecustomize.pl file:
                do $filename unless $filename =~ /sitecustomize\.pl$/;
            }
        }
        delete $require_list{$filename};
    }

    # }}}

    last unless $buf eq "PK\003\004";
    $start_pos = (tell _FH) - 4;
}
# }}}

# Argument processing {{{
my @par_args;
my ($out, $bundle, $logfh, $cache_name);

delete $ENV{PAR_APP_REUSE}; # sanitize (REUSE may be a security problem)

$quiet = 0 unless $ENV{PAR_DEBUG};
# Don't swallow arguments for compiled executables without --par-options
if (!$start_pos or ($ARGV[0] eq '--par-options' && shift)) {
    my %dist_cmd = qw(
        p   blib_to_par
        i   install_par
        u   uninstall_par
        s   sign_par
        v   verify_par
    );

    # if the app is invoked as "appname --par-options --reuse PROGRAM @PROG_ARGV",
    # use the app to run the given perl code instead of anything from the
    # app itself (but still set up the normal app environment and @INC)
    if (@ARGV and $ARGV[0] eq '--reuse') {
        shift @ARGV;
        $ENV{PAR_APP_REUSE} = shift @ARGV;
    }
    else { # normal parl behaviour

        my @add_to_inc;
        while (@ARGV) {
            $ARGV[0] =~ /^-([AIMOBLbqpiusTv])(.*)/ or last;

            if ($1 eq 'I') {
                push @add_to_inc, $2;
            }
            elsif ($1 eq 'M') {
                eval "use $2";
            }
            elsif ($1 eq 'A') {
                unshift @par_args, $2;
            }
            elsif ($1 eq 'O') {
                $out = $2;
            }
            elsif ($1 eq 'b') {
                $bundle = 'site';
            }
            elsif ($1 eq 'B') {
                $bundle = 'all';
            }
            elsif ($1 eq 'q') {
                $quiet = 1;
            }
            elsif ($1 eq 'L') {
                open $logfh, ">>", $2 or die "XXX: Cannot open log: $!";
            }
            elsif ($1 eq 'T') {
                $cache_name = $2;
            }

            shift(@ARGV);

            if (my $cmd = $dist_cmd{$1}) {
                delete $ENV{'PAR_TEMP'};
                init_inc();
                require PAR::Dist;
                &{"PAR::Dist::$cmd"}() unless @ARGV;
                &{"PAR::Dist::$cmd"}($_) for @ARGV;
                exit;
            }
        }

        unshift @INC, @add_to_inc;
    }
}

# XXX -- add --par-debug support!

# }}}

# Output mode (-O) handling {{{
if ($out) {
    {
        #local $INC{'Cwd.pm'} = __FILE__ if $^O ne 'MSWin32';
        require IO::File;
        require Archive::Zip;
    }

    my $par = shift(@ARGV);
    my $zip;


    if (defined $par) {
        # increase the chunk size for Archive::Zip so that it will find the EOCD
        # even if more stuff has been appended to the .par
        Archive::Zip::setChunkSize(128*1024);

        open my $fh, '<', $par or die "Cannot find '$par': $!";
        binmode($fh);
        bless($fh, 'IO::File');

        $zip = Archive::Zip->new;
        ( $zip->readFromFileHandle($fh, $par) == Archive::Zip::AZ_OK() )
            or die "Read '$par' error: $!";
    }


    my %env = do {
        if ($zip and my $meta = $zip->contents('META.yml')) {
            $meta =~ s/.*^par:$//ms;
            $meta =~ s/^\S.*//ms;
            $meta =~ /^  ([^:]+): (.+)$/mg;
        }
    };

    # Open input and output files {{{
    local $/ = \4;

    if (defined $par) {
        open PAR, '<', $par or die "$!: $par";
        binmode(PAR);
        die "$par is not a PAR file" unless <PAR> eq "PK\003\004";
    }

    CreatePath($out) ;
    
    my $fh = IO::File->new(
        $out,
        IO::File::O_CREAT() | IO::File::O_WRONLY() | IO::File::O_TRUNC(),
        0777,
    ) or die $!;
    binmode($fh);

    $/ = (defined $data_pos) ? \$data_pos : undef;
    seek _FH, 0, 0;
    my $loader = scalar <_FH>;
    if (!$ENV{PAR_VERBATIM} and $loader =~ /^(?:#!|\@rem)/) {
        require PAR::Filter::PodStrip;
        PAR::Filter::PodStrip->new->apply(\$loader, $0)
    }
    foreach my $key (sort keys %env) {
        my $val = $env{$key} or next;
        $val = eval $val if $val =~ /^['"]/;
        my $magic = "__ENV_PAR_" . uc($key) . "__";
        my $set = "PAR_" . uc($key) . "=$val";
        $loader =~ s{$magic( +)}{
            $magic . $set . (' ' x (length($1) - length($set)))
        }eg;
    }
    $fh->print($loader);
    $/ = undef;
    # }}}

    # Write bundled modules {{{
    if ($bundle) {
        require PAR::Heavy;
        PAR::Heavy::_init_dynaloader();
        init_inc();

        require_modules();

        my @inc = grep { !/BSDPAN/ } 
                       grep {
                           ($bundle ne 'site') or
                           ($_ ne $Config::Config{archlibexp} and
                           $_ ne $Config::Config{privlibexp});
                       } @INC;

        # Now determine the files loaded above by require_modules():
        # Perl source files are found in values %INC and DLLs are
        # found in @DynaLoader::dl_shared_objects.
        my %files;
        $files{$_}++ for @DynaLoader::dl_shared_objects, values %INC;

        my $lib_ext = $Config::Config{lib_ext};
        my %written;

        foreach (sort keys %files) {
            my ($name, $file);

            foreach my $dir (@inc) {
                if ($name = $PAR::Heavy::FullCache{$_}) {
                    $file = $_;
                    last;
                }
                elsif (/^(\Q$dir\E\/(.*[^Cc]))\Z/i) {
                    ($file, $name) = ($1, $2);
                    last;
                }
                elsif (m!^/loader/[^/]+/(.*[^Cc])\Z!) {
                    if (my $ref = $PAR::Heavy::ModuleCache{$1}) {
                        ($file, $name) = ($ref, $1);
                        last;
                    }
                    elsif (-f "$dir/$1") {
                        ($file, $name) = ("$dir/$1", $1);
                        last;
                    }
                }
            }

            next unless defined $name and not $written{$name}++;
            next if !ref($file) and $file =~ /\.\Q$lib_ext\E$/;
            outs( join "",
                qq(Packing "), ref $file ? $file->{name} : $file,
                qq("...)
            );

            my $content;
            if (ref($file)) {
                $content = $file->{buf};
            }
            else {
                open FILE, '<', $file or die "Can't open $file: $!";
                binmode(FILE);
                $content = <FILE>;
                close FILE;

                PAR::Filter::PodStrip->new->apply(\$content, $file)
                    if !$ENV{PAR_VERBATIM} and $name =~ /\.(?:pm|ix|al)$/i;

                PAR::Filter::PatchContent->new->apply(\$content, $file, $name);
            }

            outs(qq(Written as "$name"));
            $fh->print("FILE");
            $fh->print(pack('N', length($name) + 9));
            $fh->print(sprintf(
                "%08x/%s", Archive::Zip::computeCRC32($content), $name
            ));
            $fh->print(pack('N', length($content)));
            $fh->print($content);
        }
    }
    # }}}

    # Now write out the PAR and magic strings {{{
    $zip->writeToFileHandle($fh) if $zip;

    $cache_name = substr $cache_name, 0, 40;
    if (!$cache_name and my $mtime = (stat($out))[9]) {
        my $ctx = eval { require Digest::SHA; Digest::SHA->new(1) }
            || eval { require Digest::SHA1; Digest::SHA1->new }
            || eval { require Digest::MD5; Digest::MD5->new };

        # Workaround for bug in Digest::SHA 5.38 and 5.39
        my $sha_version = eval { $Digest::SHA::VERSION } || 0;
        if ($sha_version eq '5.38' or $sha_version eq '5.39') {
            $ctx->addfile($out, "b") if ($ctx);
        }
        else {
            if ($ctx and open(my $fh, "<$out")) {
                binmode($fh);
                $ctx->addfile($fh);
                close($fh);
            }
        }

        $cache_name = $ctx ? $ctx->hexdigest : $mtime;
    }
    $cache_name .= "\0" x (41 - length $cache_name);
    $cache_name .= "CACHE";
    $fh->print($cache_name);
    $fh->print(pack('N', $fh->tell - length($loader)));
    $fh->print("\nPAR.pm\n");
    $fh->close;
    chmod 0755, $out;
    # }}}

    exit;
}
# }}}

# Prepare $progname into PAR file cache {{{
{
    last unless defined $start_pos;

    _fix_progname();

    # Now load the PAR file and put it into PAR::LibCache {{{
    require PAR;
    PAR::Heavy::_init_dynaloader();


    {
        #local $INC{'Cwd.pm'} = __FILE__ if $^O ne 'MSWin32';
        require File::Find;
        require Archive::Zip;
    }
    my $zip = Archive::Zip->new;
    my $fh = IO::File->new;
    $fh->fdopen(fileno(_FH), 'r') or die "$!: $@";
    $zip->readFromFileHandle($fh, $progname) == Archive::Zip::AZ_OK() or die "$!: $@";

    push @PAR::LibCache, $zip;
    $PAR::LibCache{$progname} = $zip;

    $quiet = !$ENV{PAR_DEBUG};
    outs(qq(\$ENV{PAR_TEMP} = "$ENV{PAR_TEMP}"));

    if (defined $ENV{PAR_TEMP}) { # should be set at this point!
        foreach my $member ( $zip->members ) {
            next if $member->isDirectory;
            my $member_name = $member->fileName;
            next unless $member_name =~ m{
                ^
                /?shlib/
                (?:$Config::Config{version}/)?
                (?:$Config::Config{archname}/)?
                ([^/]+)
                $
            }x;
            my $extract_name = $1;
            my $dest_name = File::Spec->catfile($ENV{PAR_TEMP}, $extract_name);
            if (-f $dest_name && -s _ == $member->uncompressedSize()) {
                outs(qq(Skipping "$member_name" since it already exists at "$dest_name"));
            } else {
                outs(qq(Extracting "$member_name" to "$dest_name"));
                $member->extractToFileNamed($dest_name);
                chmod(0555, $dest_name) if $^O eq "hpux";
            }
        }
    }
    # }}}
}
# }}}

# If there's no main.pl to run, show usage {{{
unless ($PAR::LibCache{$progname}) {
    die << "." unless @ARGV;
Usage: $0 [ -Alib.par ] [ -Idir ] [ -Mmodule ] [ src.par ] [ program.pl ]
       $0 [ -B|-b ] [-Ooutfile] src.par
.
    $ENV{PAR_PROGNAME} = $progname = $0 = shift(@ARGV);
}
# }}}

sub CreatePath {
    my ($name) = @_;
    
    require File::Basename;
    my ($basename, $path, $ext) = File::Basename::fileparse($name, ('\..*'));
    
    require File::Path;
    
    File::Path::mkpath($path) unless(-e $path); # mkpath dies with error
}

sub require_modules {
    #local $INC{'Cwd.pm'} = __FILE__ if $^O ne 'MSWin32';

    require lib;
    require DynaLoader;
    require integer;
    require strict;
    require warnings;
    require vars;
    require Carp;
    require Carp::Heavy;
    require Errno;
    require Exporter::Heavy;
    require Exporter;
    require Fcntl;
    require File::Temp;
    require File::Spec;
    require XSLoader;
    require Config;
    require IO::Handle;
    require IO::File;
    require Compress::Zlib;
    require Archive::Zip;
    require PAR;
    require PAR::Heavy;
    require PAR::Dist;
    require PAR::Filter::PodStrip;
    require PAR::Filter::PatchContent;
    require attributes;
    eval { require Cwd };
    eval { require Win32 };
    eval { require Scalar::Util };
    eval { require Archive::Unzip::Burst };
    eval { require Tie::Hash::NamedCapture };
    eval { require PerlIO; require PerlIO::scalar };
    eval { require utf8 };
}

# The C version of this code appears in myldr/mktmpdir.c
# This code also lives in PAR::SetupTemp as set_par_temp_env!
sub _set_par_temp {
    if (defined $ENV{PAR_TEMP} and $ENV{PAR_TEMP} =~ /(.+)/) {
        $par_temp = $1;
        return;
    }

    foreach my $path (
        (map $ENV{$_}, qw( PAR_TMPDIR TMPDIR TEMPDIR TEMP TMP )),
        qw( C:\\TEMP /tmp . )
    ) {
        next unless defined $path and -d $path and -w $path;
        my $username;
        my $pwuid;
        # does not work everywhere:
        eval {($pwuid) = getpwuid($>) if defined $>;};

        if ( defined(&Win32::LoginName) ) {
            $username = &Win32::LoginName;
        }
        elsif (defined $pwuid) {
            $username = $pwuid;
        }
        else {
            $username = $ENV{USERNAME} || $ENV{USER} || 'SYSTEM';
        }
        $username =~ s/\W/_/g;

        my $stmpdir = "$path$Config{_delim}par-".unpack("H*", $username);
        mkdir $stmpdir, 0755;
        if (!$ENV{PAR_CLEAN} and my $mtime = (stat($progname))[9]) {
            open (my $fh, "<". $progname);
            seek $fh, -18, 2;
            sysread $fh, my $buf, 6;
            if ($buf eq "\0CACHE") {
                seek $fh, -58, 2;
                sysread $fh, $buf, 41;
                $buf =~ s/\0//g;
                $stmpdir .= "$Config{_delim}cache-" . $buf;
            }
            else {
                my $ctx = eval { require Digest::SHA; Digest::SHA->new(1) }
                    || eval { require Digest::SHA1; Digest::SHA1->new }
                    || eval { require Digest::MD5; Digest::MD5->new };

                # Workaround for bug in Digest::SHA 5.38 and 5.39
                my $sha_version = eval { $Digest::SHA::VERSION } || 0;
                if ($sha_version eq '5.38' or $sha_version eq '5.39') {
                    $ctx->addfile($progname, "b") if ($ctx);
                }
                else {
                    if ($ctx and open(my $fh, "<$progname")) {
                        binmode($fh);
                        $ctx->addfile($fh);
                        close($fh);
                    }
                }

                $stmpdir .= "$Config{_delim}cache-" . ( $ctx ? $ctx->hexdigest : $mtime );
            }
            close($fh);
        }
        else {
            $ENV{PAR_CLEAN} = 1;
            $stmpdir .= "$Config{_delim}temp-$$";
        }

        $ENV{PAR_TEMP} = $stmpdir;
        mkdir $stmpdir, 0755;
        last;
    }

    $par_temp = $1 if $ENV{PAR_TEMP} and $ENV{PAR_TEMP} =~ /(.+)/;
}


# check if $name (relative to $par_temp) already exists;
# if not, create a file with a unique temporary name, 
# fill it with $contents, set its file mode to $mode if present;
# finaly rename it to $name; 
# in any case return the absolute filename
sub _tempfile {
    my ($name, $contents, $mode) = @_;

    my $fullname = "$par_temp/$name";
    unless (-e $fullname) {
        my $tempname = "$fullname.$$";

        open my $fh, '>', $tempname or die "can't write $tempname: $!";
        binmode $fh;
        print $fh $contents;
        close $fh;
        chmod $mode, $tempname if defined $mode;

        rename($tempname, $fullname) or unlink($tempname);
        # NOTE: The rename() error presumably is something like ETXTBSY 
        # (scenario: another process was faster at extraction $fullname
        # than us and is already using it in some way); anyway, 
        # let's assume $fullname is "good" and clean up our copy.
    }

    return $fullname;
}

# same code lives in PAR::SetupProgname::set_progname
sub _set_progname {
    if (defined $ENV{PAR_PROGNAME} and $ENV{PAR_PROGNAME} =~ /(.+)/) {
        $progname = $1;
    }

    $progname ||= $0;

    if ($ENV{PAR_TEMP} and index($progname, $ENV{PAR_TEMP}) >= 0) {
        $progname = substr($progname, rindex($progname, $Config{_delim}) + 1);
    }

    if (!$ENV{PAR_PROGNAME} or index($progname, $Config{_delim}) >= 0) {
        if (open my $fh, '<', $progname) {
            return if -s $fh;
        }
        if (-s "$progname$Config{_exe}") {
            $progname .= $Config{_exe};
            return;
        }
    }

    foreach my $dir (split /\Q$Config{path_sep}\E/, $ENV{PATH}) {
        next if exists $ENV{PAR_TEMP} and $dir eq $ENV{PAR_TEMP};
        $dir =~ s/\Q$Config{_delim}\E$//;
        (($progname = "$dir$Config{_delim}$progname$Config{_exe}"), last)
            if -s "$dir$Config{_delim}$progname$Config{_exe}";
        (($progname = "$dir$Config{_delim}$progname"), last)
            if -s "$dir$Config{_delim}$progname";
    }
}

sub _fix_progname {
    $0 = $progname ||= $ENV{PAR_PROGNAME};
    if (index($progname, $Config{_delim}) < 0) {
        $progname = ".$Config{_delim}$progname";
    }

    # XXX - hack to make PWD work
    my $pwd = (defined &Cwd::getcwd) ? Cwd::getcwd()
                : ((defined &Win32::GetCwd) ? Win32::GetCwd() : `pwd`);
    chomp($pwd);
    $progname =~ s/^(?=\.\.?\Q$Config{_delim}\E)/$pwd$Config{_delim}/;

    $ENV{PAR_PROGNAME} = $progname;
}

sub _par_init_env {
    if ( $ENV{PAR_INITIALIZED}++ == 1 ) {
        return;
    } else {
        $ENV{PAR_INITIALIZED} = 2;
    }

    for (qw( SPAWNED TEMP CLEAN DEBUG CACHE PROGNAME ARGC ARGV_0 ) ) {
        delete $ENV{'PAR_'.$_};
    }
    for (qw/ TMPDIR TEMP CLEAN DEBUG /) {
        $ENV{'PAR_'.$_} = $ENV{'PAR_GLOBAL_'.$_} if exists $ENV{'PAR_GLOBAL_'.$_};
    }

    my $par_clean = "__ENV_PAR_CLEAN__               ";

    if ($ENV{PAR_TEMP}) {
        delete $ENV{PAR_CLEAN};
    }
    elsif (!exists $ENV{PAR_GLOBAL_CLEAN}) {
        my $value = substr($par_clean, 12 + length("CLEAN"));
        $ENV{PAR_CLEAN} = $1 if $value =~ /^PAR_CLEAN=(\S+)/;
    }
}

sub outs {
    return if $quiet;
    if ($logfh) {
        print $logfh "@_\n";
    }
    else {
        print "@_\n";
    }
}

sub init_inc {
    require Config;
    push @INC, grep defined, map $Config::Config{$_}, qw(
        archlibexp privlibexp sitearchexp sitelibexp
        vendorarchexp vendorlibexp
    );
}

########################################################################
# The main package for script execution

package main;

require PAR;
unshift @INC, \&PAR::find_par;
PAR->import(@par_args);

die qq(par.pl: Can't open perl script "$progname": No such file or directory\n)
    unless -e $progname;

do $progname;
CORE::exit($1) if ($@ =~/^_TK_EXIT_\((\d+)\)/);
die $@ if $@;

};

$::__ERROR = $@ if $@;
}

CORE::exit($1) if ($::__ERROR =~/^_TK_EXIT_\((\d+)\)/);
die $::__ERROR if $::__ERROR;

1;

#line 1010

__END__
PK     -KØP               lib/PK     -KØP               script/PK    -KØP×LQµ;  Ã     MANIFESTu½nƒ0…wâ&Q4ÂîĞ	CªV
YÊÔÉ€	®ŒMm§ITõİM;Ùç#Ÿï €Ì‚ hU1cx)PT‡,ÄoÓlzªg8K·Oëç¸„Ju½àTZ(µ:¦AâL'[ç):wÂ¼ÄEšmpÎåõc*Í{‹÷šõõbÒår”,€´¶	)U}%…¢u<ÿ¢vq­ªCÇ¤E£‡¸”L?æÙ™ÇzøUb?“EìŞhÚ10ºŠ—Ÿe£‰ê¢ÀØ³`W«Jl¨ù¾µQËÆ#¼¿»‰¼¶íx»¤‚/&ä 7j”öÆ¶Óø';ÿ›7Şl÷rzCµUëá÷Ws»Àşrù7Lîmë«Mğ„Ph5k® îjÌ¬ÜkÑ$	¦‰ûóÏÖq1O~PK    -KØP#“/S¡   Ù      META.yml-Í1Â0Ğ=§ğÖ¨K6nPq(IMdµu‹“€*Äİq)£ı¿ŸC¥±w‚J‚ÙÂûcâÌ÷‘bÙ§rqì'´ˆIpÉ¿¥P¨…fve]4\¼˜~Õ"E·	”,œLBFñ{VMw½YÛù8 À%ë9´‡Ó¹mŒ~DÎ
Ux~±QĞ€8¢çÈ”Ø—*ZjU¾Ğ´§Ïn`{1_PK    -KØP‚L"å  za     lib/YAML/Tiny.pmí<k[ã¶Òßó+„É®È…ĞÓ–†åVZîÙ=İ}0Íq…¸8vê—ıígf$Û’í„ìéö}¿4ÛšÄ–F£ÑhnÏªëxœu˜ñéàŸïÚïï¡5›•8äìëÖÆÆÖÆFg›­²Ğ†VŞ7·XÏf~Q“0
œa´MßïìÀs¼ëp»2³‡7ö5g²ÛE˜âÚ‰Øˆ‡ÃÀ™EïuÙm§õífóÛæõÖ7[›ão¾ª¬²?6ONøÄœ†ßjuXäûÌwGíAàßpı€ÅÑxkO6şøööÎö†vñ€}/ä^‡€LÈ¢‰1¿‡ñxìîE,ö"Ç…î³À¿ˆ~4áÁòJÅVı÷ÑEïäì”í0ğûÊÜ®TV¿ÄF|?QiÂÎOZyègPælàé>°‘?Œ§ğ•°-ÇŞ)¶§<šø£ÙŞºó{\°[¡e8¢_<Ø¦éìŸô˜øì°ßîÓÇŒ=ËGÏÏ.Şg-Şùöˆ½§³\‹şÙOJ‹cÇåÔŠ¾ŒÎGôí;ìUó=–Ûn[‹Ó 4Ãx x¬à°âÀS(ÓÜõø]m¿_oîöGĞ®læ]oW+*\P?xÀ\Xş”Z,ä|
´a˜Ç€Oì[ÇZ
¿œ wÃÆN İîüØQ6õa	< Únƒâš|ÏŒØçßÉQ&ğÌfÂ†Û»Fğ1ÎŠh'f5}`Õ»c ™:³¾Mä„pŠÛÔØ³ğ©ÙA`?°º„¡PgŸ€‰ÖÏŒ»°ÒY`!ŸMíh8¡±`7’~ÈQyò€	ns÷²Ù¹’Ğsd~ë³;­mXÒãÀŒrµûhÅ÷4Ú
‚	
‡~‹±s”ÈØÿ³€íi˜M[•ï~89•SY“€w˜õùCà³F\Åè.Ò—Ø a!â¾Œàcü¹ÃÂ‰3¶—d.ìS£õtÊá‹@/\aêYbÁîÌ÷`
«
r2Ç Ë­³Ü©gƒ_ù0bg“Š›.2Á!,{„+Íøt=¨ÒË§.*£ü<A‘…Ì‚rèàââà“l…rzöş¨üqÍI(ÃI¶˜àÚV ÀÇqˆÜÌåù1áöıÙq]±ã8ğ=ÜâÈş	$Zƒ”CÀo~'äj).L·-€yàò(n³[< ş®ÛÌD/À1€óÇÊôğµ‰‰Œì)4¸¾æ¡ „ÁeS M”, àßyììŒd=è?èf\'œÀz„>É ¤BÁ Ù8|ãÔAB698bƒÆŠî|âg`y…•‡À:¡ÎË—Ã­K¶ßgWÙ"/oO"Ô™@
×™‡HRà9"=.­`Ú7®| =A˜jÔ¾œˆ›©Ö»|”£ö”‡-`u£òglİ±•-”ŸÂ‘}ÃÙÈlF>9ò[LÊAŒ?ˆ!èäjq€…¦¥Î8(ˆüvè çÇ=ç+y8l°»‰Ì†*¨„&îxÂqÊaƒ·ØA·‰CR°˜²—Ë¯íáû-v‚FÙMD;ÿFn	à,à§1hiXDá3{4«ˆÄkñûk,±"_ŒC6-9¶“z¦¨‘”•—r¡¨—¤(¤¡>¢±5@ˆĞò˜ü,à±<ZÿÊ—³á#Xä·Æ9LÅ%)ÊdØ›á]Ø\aAæHUìßÁædî  }z‡''JËƒ=@ò™Œ°¡ØÌÜ­
ÌÿÃéùÅÉéûƒïß‘YV#làå~£ƒ—M¼|…—àåk¼|Ã˜-6<^"¼xx¹ÅË˜Ô…€p„—cjzßÙÀÂì ÌÂì ÌÂì|ƒ—oeÓ-¼|‡°;¹¸wˆ—·xA¨ãJ};¥^N(ÃÁI°g<Äi¾úpzÔ;<8?êÁ$“)îì2ÃºßØ0ìwõÇ)±ô{ëkCh@;iğ-4$?¶àG”µŞøN¶ö’64¸M~àÇXi=”­ƒäÆpù£ƒ­MË2ñ7şmÈé&r¾Ô¼ë‡º0¦Pbó¡3âh!D$Ÿnm7æà³LÈ4p>r„ãA_U“|h!¥Ì} áYIŠ¯@¤(ÂŒ\„"
·òİœŸØ“ QŠ	Å( „Û±Ñh,‚©f“ô” õ
%Æ$úK«ö¯ íaÅ¦6ù¬ÚGZtÀ¨ïAĞ9]1x6Ø9òÈ?úSéòø 	icXéÌE=ƒâÜ¶ ezŒø5¿G_;ö˜ä8èä84`~pK#à³œ&ê)ıx(Q‡î®\ƒÔã±¯6 öl±Ÿ8Ÿ¡î¶ˆhî9›ÅìN¤ş*û-h[F­¶×µ¬ÖÓå/–qU_«[F	Sxx2Şù1`Ğ—°#ûAè±&ºË¢ÿv .W€‰ LÑß´Lñû›Ğ5[oX00Ä@o¶ã"=BpÚ¹Ğ­¸Â¤QbUg30_rc'€ú7°übl¹{µw×­üÁŸkO­µ½ÚŞN·^$à‹®Y]D.\ªÖëÙ¤Dú’„í* SskµµVßË:ÀÀ}âï~Èg°> ½“0ˆ¯Ôƒ®_T(ñ¹±¶…|.:õ nÑJõ5Œe÷š?~p#7Ø¼a.¡áë‚1åLy+µÖÑ;.Mã9S]Ø+Â0û!)ĞL¯–˜‚ÀÊ°M/7®Ø}'µÊº‰z­äpÂ‡7„/A-sP(	¨ ‰yøA™ŸüÌRaĞ“?HÌÈâ3™ô‘òòÁLÀ$›“@ğ{Ğ«§.Nì‘ùÚä—9À8d‹À3ÂPØ”Á³şx'UšEè„¡°P}aìÈË URõlF’x À@œŒØMddM†Ü@Aø£Ì+Â]ë….1ˆ¬LÊ@5±"“3ŞtcÏ¹ï‚€¢Î5‚Uí$LÒ\bTÃNšWš›¯qTè+G¬œa—UWŒzâVÊi]˜T„}=IÏ×€90Lˆw‘ê 3C 4=ë5o8C$_”¬g3H¯O kuMqOLùxèEn·ûîìğ§~ïGl˜6#úÈÑCT¬è¾Š	_‘n#­hB¹ù„.y¡óAA¡	˜î"y6™¤`§FÙñÁûƒwd`ÓÜN[Ğ«ÚÎî¼ùìŠñ•ğ«ÃT÷®ÕşI&#$An½ aaÅ†®òt{³ZÀ]X.×áM]gÑ|y¶í_à›´»ê(®K¯t¸=Wğ\„È{Yæ%íÉÑÀöÒGVüã´‰cG€÷óŒP,+°PFi'•vø9œYÆ[)-1H¶a21¤#NËÈXEN,¡ĞÀ,dMìm•©Ü jnšRäv¢xŞS 9ÓÑA‡–µ®[¨™wŠ¯Ã®;’rˆé"GY”!–Åûİ®öñKMNºÎ^¿f+òĞÉeOJéñæÙjµ`ÇÈÈ  Gİ’#	A›š@pêÜóä	
Ì­æÀ‰²©³GõV)ü€z	e+B×µï™AS1`˜;NoœÈb£ J÷*€UùBr İÿÁs¼:úÜGg+Àoå‰ÆÆıÖFsã~<ÎØ…HÏ®Põ)q¶‹Kİq’d¨‘­àÊ]/ğıÙ?³­™ğé,lÿbİ??·ÛsÀ†ş4s† ÂÒ˜bÂÚ.ØŞ@Ãj¬W`öf®e‚¸vA=Zº›è:àè5¬0À1\KM8ë÷6˜å>!&q£óõc§±ù_6Ÿğ^6ëíF9ND0ÄÉñÀ­¶E M8ÚVi;‰R¸J_Q\ ı µWØş²Ë®.­‘ÕºZo­!†´$HDWmÌæ9ƒÙÛ`²†Ê8(E¯Ÿ†swØF¶Ãï&$dò;dU†èÉÍ³³°˜ÊÖ–ödn&ÍfSĞ¹ÖZ¯Ã7Aêü(b¤a~€8èà*¥C…ÅeÑHJX¤‚°#ÈÛa+ˆXïõ§}ëó°ÂÏ,?MĞ¥ğ£‡ºh
`ÆU;`]Bü´2ñÆÇï£â“"cæ–³£÷QdEJ•dÑ„Ö—ˆ«ôdµğ_}Şâ {Ù(èG=eT.Î$Çl:ç¯H~™¿‹şE2mlWr¼ıñãG
Œ)f6×«dÁ“ô… j6eh–{àZÅ3p\…±œE‰8,hé«§-r4„@M	rÍ¡xz#ÎÓ~`¬¢÷ÌïA:âB%¯xcÅJÓ$²º0˜a¦ô7í9xç…¸ÙMrŸªO@²ù,Ê›Î£l!Ÿß/b‹ÂIY£KvU\YµÒÖÅvÚ¤¡kYsÜ‡¹=¸Ü„k(·¬Şü¦Ğäs&ù˜Şz’8rnBUÖªú‹“å¥óéMÓ&à6¶R]Ej öCÄ)¦Ó$Õµm(‰ïˆœùq(Å¡² YG`\”@ğ|¯™hôäQ02¥ˆˆ(&a1 sd°…Ğç»uìp6zÒî ¦ %PPààa:åh‰	l¬^¥¤{Ú-h€0‚^*¿Ôö«èß3 ñÒçÜ¼ÏcfïğàİÁ…©{N’k„ãT}ÄÆÏ‰w”1>‚XĞşè•j«¥§)I O3÷¨–pwbï°ı¾v”nšó,>bcb.íëm“,CÃEDDÿ*\,£m´OR2†–U»<õÀ(,klÑïƒ«§ûÚåFó;»9>h_=n>×ëŠ{¬e;y·SßÃÄ§šñã¦Ñ¨nÖ»Õôìâ±Úy~¾æ÷sÉ bzÀü2ŞNÖUæñ!/‚÷ƒ$¿N4P\Ñcå¨ÕF$|3'¤”ÿûÀ™f¡àl7—P1D+·ö<BŞEôÛdR¨sFğmyÿ3íŞÜGËŸíeÄö/s#ŞeQã¼‰¨ç>ä˜W-y+8Zµ–@H‹áÿ	„œ„z÷«ˆĞ%l3cåõ•>RAÆ¥áÓ$iÁfcÉNè%¢NÀ5ÓØ—‚óã³º4òã³›‡¹j~yUè`]ZW….rªü:F&NÏ(d(§0i2B/wŸ^šğØ1+ÁÅcw7w;sm¸ŠmÓUæ·Ê5ìÑ
—FUıéÒÚ·^Yÿ¹ÓÊ´n¹MÓî&}”ø^nS­¯¶ä¾*0Š"ÏR® ğ^¥|Ò©&ƒyÄ¦±9MZ\)#d$ÍHç~UõSÕ¿˜ÛM„$ –Í]´º˜bwI´¥Œ9F `¤lDKÍJı¾¬ùæ•ğ34 ovrM–XlLù}(ÇÓ48!næÃºç±<‚ÉL$Fá¨÷«IôS×¨ZtÄUÚ(ƒJ×Ò”Ù»;ú´³ÂØLñhÀ¾ 7ÔÈÃªIê½».?Ğù' »¦ê`£Á`lÆ»fí1şu™ayFfÂDe;iÇ¦èh2­£dÛ_}ÇƒûkC%dµ ª¦ï¤÷¡h2áİ_¬Ç-.£Dš‚“¬¸ê2¾àyçÍüed±Ç,½ådªtWYÒU‰"”„}•Më‚ã.Ïœ4w…+ßäöb÷tRUeîÀüé]œ#Äœ%$o„lÃ-ãÊê­eGÄOeÎñ*;ñhP’÷È{Ôˆx¢¸Y*éæP>l#´õ&Ò©”^ğ;Ë|Ij+hŠîä~¶+œ®’¢y•y÷Y!’¤ÑZYm—¦Çe›®|†sâNE–‹«şš¿¾ukÎ[v¡2ğºÛÉºÏy­²SÔ!BrAË è&/M…¹>¿ˆâ{!^´:ŠÃ”ÆjğS‡)mGm?ƒßJ”Ğúyq`§°¼sB;/lÂLi•dî$U¦Y0Ñ¥Ã7ËDJ¼ı^ÚÕDÀZk}¯>g¯²ƒŒwe¬Gä-+ôe(‹³¨`T7Îi¾„ÀåRlYSrg‘))(ğ^fé¦ùËtâÉ?r™0<ö]×¿­¤û	  ãswÆ¾ßÍ7¢äÎÜÍº Er¯æšÁ˜îbÚ––”{ÌEP“œâT	¾Ó)M.ğœIX”yıÓüù\~êèqR¨™ÕGÈ>¼ı·Í÷·Í·Èæ[eÇ'2Aox¦yQïÃoíøõ_"® ·ÃE =¢ƒP%™ˆív
$?Ä
CÃ/„¤tv(ğ/ ZN}Dó¡ª/€¨šLú¿ã™³Åm^©ª¡•—°•zs¯¸}ÿ·˜Ú"!)±øD°Îá”2
‰ÚÜ}Dâ<å%Ç¶¦ÍF1cb¾ÕÄŞæçíQ-CÖvOG0‰ß++QTÒŸrV>å|üä3LtjekF‡©Ğõò“ükü§9Ê*?øe‰;±¬+¡Á*5±åè|[›Ú.Cë/qw—wÉ¨™ï.¹®=Â<ŒÇÿ‡Ú-ç@ş«„ŸR¯ü×|[Q&»ã[¾…d÷	’4÷ßSİÓÀæ¾.&GÃ7ñ¸ÈşìwíÔu¹÷Ød[ö”$-“ÌÑµÃ×jÀ±ñ†"}¼&¿Ó›7hË1I1ËöMQvíkLÎ“)ÖgıŸ/ÎNß}ªÕŸÒ;‡GïkÊÚ‡¡HI§äl"a#TÈÌÖ	º(Ûœ¯&‰Úz^ä‡çsä“É$¡>m=p¼©?âC£Øw…$Oú¼dó£>Ù\™Bº&„ø<-å?iÂEI9¿‘7Óûb“åäÑUÌ{wá›¹p0Ûu~W°“GÔÂí¤fô${ğÏe/†¦»íË¤œÏc¥2Îsj™hQ“Ê•Y,|µ¡@ùÔ.Í*KÉp›sWÄ›ÉZfe¦³²ÄĞ,cVP‰5Ö³ÎË0ßFä½ÇALW›—Ñ&b8²ÁLp_ÍB.ŞAR 4ÃP&Ğ¥A9`yF„ Ü&HÖü ùìÒ9ZEPtÁ(2qÕvk‡Î¸Z9f”9 )âŒ)`37q,ÃFÅÇ¨üYŠ–bõ-@¾l—Wf¹~_6)U[cI*ÀH	º<>—’ea>İ|²üxĞûñª¼ú\ª<>ÿeT‘ç-Ëeq¸·fÚÙ©ô4€<5	½7oó&’r„NTİ_>‘l¿$lQ—}]Dâq.H²†|Ÿ×¨ö-Ï `Išo’Èäê'o“H)yERŠß…B*ƒ>\¸ı}·ŞÂüú›B ×,Ñ£¾-Bª 5É)¥ì}ù¯*o æîñ»ƒzì5Âëı{Ü??û©–ÕPw¿*Ä•×x’“x½a_Æú—›W‰é–ŠNÈ-âªG$œY@ËO92+ó•é)F“¥²’‚Ä“4C	†›OOåeîæfºr¯iíÓ4rysz;äÈé~ˆ°r÷°˜÷½x:àiJ@]731¶Fv|õœŞs%·Àñ-ÇP¼‡$ß¸¦ìUûÑ£	ÉŸŠt™—üí·Ë4GqR†YÊ’l”—m85ç«4±h –À/#øŸÃ—ÎØºÿv_¾[¦ååò“rÙˆğ\”Aµçå+Î{jyğØ›ûøë\A“Ó¹MjÉ,:ã«:´¬*å$.ı`„NŞÕÂŞr¢ĞÛ´îQ³†dókÆ«FC‚¨·¯y#]©'C‚3¶‘º¶×ıåò•ıÕW¯×v÷ºÓøÏã3&‘]=ıÒ\¯>YáS×ú]OÁªRñG	G@.IÁ3&»rT3%¹Lr
áš®*gwÁ³éfF°ƒ¨[ıy}ı3÷†N0¤T¹´ A¨Í¡`âMÕä.YÅJTø0IzYb²ûdR˜¼c6EK)B3¼HÉİÜÑÂŠ|^ˆ×Ò /d€aNêJÿ–'6Ğ˜,0jµOÌ=OÖGYÜ¦ÌˆânÊls¦héKXeƒB¡R‹ï%$ËÏ†SÂ”Û`ÔˆÄ÷«¿†8Ò–úr´)µû>—6/FŞ»O²°õpeˆ¼q”a‰ƒÎåäöúÿĞ|B&9†æ•—$ÀF"(b¿g]‚Ì6er Pb‹]ãoaô·0ú[}®0úrpŞ_¼?zK%em
Ğ‹II½šÒ¬Ì:­¯¿­‹‚»àr‚±…H©Ü.ë[@¨œâ½ÍGiŸ‰ ı¡Èú 	 ÍmKo¢¹Šq
«·.+­Ñzk-9F(İî0ğí›ZÚ')1qo›qp]*òµ.hÑÇÀ.mv²Ó<ìäp­©G«Í]Ù}%«š=èvå'L«JòQI	—õumŞ_ºêİÜÕaZì\œJ<hE:‘j~§šf†O½[ò‚o©4ñ÷Ûz=ÉPd³71LÎ°òÄÀqè+‡ö°ÁÌµ#¬â»µf${v›µ’Â¾àt;€"ûKœ%EPĞòĞ€"ÂXÏ‘3?¾È
‹qNj£á{`éÙ ;çş#ÔcßÇ¢œîCŠ» îĞŸ Rx‚bš¡pòCN’}TÄ²–ÛrÂ0æ¢dZaÿ[eG9’µô„·´uéÛE
¬LääKÊª¾7v®uU:Äb»¯Ä“n7ßB<-ÖP¢‚ï?S·öHbñ„æ"oàŸ±ö‹rGoÜä_8/:&Ô8J¼zê‹*!¾{Ğ%Œ§ş-§jÉb5Ğr©³…çxt^CEñ¨F+Új`úq¥²} å‹m/Ü3@‡„õY…rÆ§1•Tu"Që[ëƒ–šZMYR_A§Z·æ®,x^Ã©×·™²&MXé5="$,à0íÍóèômÿüèâ‰è4”³¬ª Š»G”{—CÅĞY€nvs-,¼Ú©3:DËŞ4T¦º²Â>œàÜŞ¡üõD—3Q~è§£¢°Oò\Ÿæ±}ÃM%xªNPâ£-è`(ƒB÷5ën=É»£h"tù°Ÿ•†2Q›ãñ¯¹Í&ü«aHKYCF„ù¥r ~KÁï ÌOHmw9ª&ñ °êbg;Wì{ˆdÙ‰9û%"yÏä;æ‡IådyF%ªHŠŠèXÀ@aY!—Û·Ø
„‡Ä`g<§p¨@Ş‰,½.’kı¤‚1VøiSİ_¼‰é¢mª?à®—œ`'…TäË±h…ˆÂb„ˆMï(Ú#H˜”–Š-‹Ú¬½O§gç½“ZúGVÃŒôZ¼(Î,€S	K*+	
*=Ø-*c¿ê–h„Êôb¿+ÓR“ÒùÇÖVå¿PK    -KØPådáĞa'        script/greps.plí=kwÛ¶’Ÿ_Ğ®EÅ¢d¥w_våØíuÒœM“Ûím7rYJ¢-P¤*Rutíoßy  À‡¥¤¹{v÷lNœ˜à`0ÀÌ`¸û¸·Ì½Q”ôæá"~ôhWÜ.Ây&\üOdËQæm,¾šF™˜/ÒÛE0ğëÍ"E–ŞäwÁ"<«t)ÆA"á$ÊòE4Zæ¡ˆr$“^º³tİ¬ -“I¸ù4y¸˜e"½¡‡¯/Â$\±x»ÅÑX¼ŠÆa’…"€–±$›†1B4Xá9Rp))ÏSÀäQštDÁû…ø#\dğ,¾VMH|‘. ‡äHöB¤s¬ÖZW"ò¢f·¦ßE÷&"Jí4C_¦€zwÅ±…b™…7Ë¸ VüíåÕ÷o~¼g¯;»¸8{}õË1ÀæÓŞ†„Œ)šÍãCA’¯€p@ğÃùÅwßC³o_¾zyõP/¿¼z}~y)¿¹gâíÙÅÕËï~|uv!ŞşxñöÍåyWˆË‰
¡şœ½¡±öMÂ<ˆâL|3ÍóùQ¯www×½M–İtqÛ‹8ët™¦³¡3 ¿…,Gİq:ëeÓ`•M£YàN=NäÙ8?¦ß¡sI”ÜfüôİİD´‚QæÏƒ|Úâ²acrtô*MnÅïwîÑ8Mn¢[ù·ôN$©İ&@¹?²°]_ßĞØfÏéìhHn%èó(¾…ºI0íbá_a â££Ë<¿¿Zãa¡õ,‡á÷âÑÎOç—/ß¼ƒávÿõk§óhçíÅ›şë³Î±4q°7ĞÀ<Xd¡»wØ¿/zÃî»_»×Ozm¨ğãåÙ‹sÿ‡Ë„æÇ,¸…)ät7Ví:âİ›·WĞşu·Û…q¿º:¿x-Ş=ùêœJŞyxqqş–.¡è\?š­Ä^eá"÷oâàVÄ>ô|‡>—ãÈèŞDÉD„æ‹0Ã)±`ãVLD#@ÜÙ6ççşÑxİ©yM€>Ix'îí AîŞ8†ÖÛ@Ì©LE{YßÀó½O¯=]Øj¡ˆ@:²JéÙ;¹÷‘r¿ |hä;’D?OË P{æËEÂ€ÇÖÈ…*ÊhB°8UfAÓ'±	Ä}
.£~`–ñc?Uş»Ãë:*k•,ì1P>u\TƒV1‰}Æ ÍÅ,õ2áq­5‰òb9ÎI‚Œ'Ø­LÎiƒò<-KÀŸã©(T	C
êrMPİ€‚äV™â¾W; û†=Zˆ¾’`±z	€bS”p^`öŠVUĞZM’R“J\>TÚJğ:Â7ëĞ³à¿M071;aSZHG™ààŠ¬çvÛİ'½½~¯hÛñœ®Itè±¦¿GûÿƒóeGq8ºˆn€Aåášõ`}Ê“^Û®ÓŞPg§Yˆ•Œ±<Boi9øÃø>\Á¿ñ2ÄÇIG³¶UÛ)Ô§gÂã3*P(}’ŠõÓÿ¤áv	Úì²k1ß¸š,x³îıÈ ë¶äUŞä°?š@•0~‡Æ—¸„Ã
kq¯­î¨2˜ÍÑ³6–ïõqùÀ9½£±ZOºˆeÍÈ^X,İSC€˜-*ôzÇÖ«âÍ^¯'I/3g fA”éÕrk*¬|¾^_İBt˜aŠğô~ÒV§PdVMs4ªtÅmèƒùëm[œˆCc¢•ñs:]NÃQÜşO˜K“òaşìÉ¾ø y}´~}·{ıîñu÷É»wGğ><ºî]·$p£XÁinĞâŸêÅfy¨Ó‚o"ƒ3Y.Lq0äOÁ­m3¥ämš»…ğÃNlÔ!…‚îP7ß1:Ò-!SÚNê èô¿÷û¹:CÇ‘“%y<9<X)f¢·<øĞ¹…ŸôàƒğÁ¯ÂáöyÂÚi:Èë‹ûµø(Û: xLÓ‰ç”1ÜvsÖ‚ó
ˆ[Â/_bŠÄ—9=:â‹ı`q»İù&…ÂC…R_#ÂÃïÿÖyäîášàkëDñ D¬X¤Nï5Ñºëk%îT"eIIÙ8]&<½ÕZe4(W*]±;¨İ}x'0Ô®ƒ Ë¦¸Fùó¢Ä8.{©phõ“TğÊGd=ÿ4ª SaÜ¢©< º*‹‹$JVt*ƒøeˆ3†×I„ãUŞ’^lu‰±URq"úæ:ª@-íPô-{àYuuüÈ°Ú¼ˆÓ«‹_Î~xutt%«²¥æÑÎÙååË¯Ï¾}uîŸÿüöâüÍ6¾4Œà|}×B©ÂE¤~¥güÿºóhçÛ—¯Ï.~JçgWo.É„v&ªõöâÇ×ÍÎË$ôT‹üÙd
+^Ò#"¾üşüÛ³×/QËqgÒ/Î¾û÷ó«Í´'	j²8#´®ÑpD–#?"¾CÔX„&QÌbÍU>v  ët÷y‘ÓF1(B8Gâ¢:9È]”å¸Â† ¨0dB,Âß—ÑÂWa<y'Ñl.r·åıc"pÓºù°\vx½’¿²=‘)GîÒÒq,ãÜgc°½hLƒlŠ5Ñj†³†aPç!±„k·Ú.¯:Í5iQå¥åĞX2k}’ƒ	ëSÿ¤¥AŠşúåşòºWa‚_b‚\Õ¬2ã„Z»ÄÉUIÌ½Ôk†±^ãÕ¢LšØÖL*4s…¯T…øêŞ„t+¨ÚkUÇàÔ’€÷ºt­å^ª€ÁıºÌ\´%\—v¬iSÕi¬·‡q1$¶PVËÚŒ¦š*ö¼É´™[ŞÓa	hm¡(XWÔö¨ÊjeNÕ˜,«,3ê›Â\Û°`4]+9Ê ƒ.ú…Øƒwşú§ûïßüp¾FuÂ•6p×é9İ:W×r¡‰
B¤>lv­Y}¿î–@øİ˜ƒC§[œG^h	‡Øß¯Ó¨XdõÃ×Ğ”©E‘X›<”×‚,à3ÖòN€ÙkKĞ†È¬´³¢<}‰_!(rBêÊÓbnŞï?¸ˆ#†ı`2QíOaÃŒy´@b3œ¥$B˜]ûD¹<äÖÎØûàìÃH‡MÄæ]ÀgS2"waÜHÎûp•ìüÜv‚­úß´Ÿùì^Ó¾f«¦›w<Ÿİ¸Úo5ò›7QŸ?ôl?ß4özŞauÔH6ë£?y%7?ª‚zâ™ª7<7)Ğ7
6Ycu÷´ŠGLTmĞÈ†N²F¤=$ÕÜsë`ÚQèöå3Û6İwÃ;ïú =x¦ŠÆ^Lkšõ±[Ù¿·¨í¯×Š=MæD‹êŠĞb7H‘I»(oÖ{†ÿä!mãRÕ®†ıëJ[µU¬Ö¸İ'mÅË¸…¨jšÏ³{} ã©e],T4’š†Sì¤4«Lc£Ğä8”­ İUÄÍÒÄÜäözuºÕ³ ß5Yv­u³ºƒ<:R»>>Í›Ë‚YÛ7vÄ¿*SÎ)¹ô3µ­-U½/Ş*xÜıÔ‚Ò‹;j°Ávú'ì»rŒªÖE98e^ÓÁIşÙÎ4K–¨Œ§n1dŸ§`hÙ'ãRŒ,c*ä0£-¹š%\½}ˆ¨Ÿ,k)Ïö Œù2+Œ“p´¼¥GAÏ9úXöAeÀ–V²"ÒğÊˆ uÊvN32…7Å;ÒÀFñlŞ—‹,úÏ_}&ñ+³ágópÌ»Q>D.3X¯€ˆå,LòLFÌ`pÓ¤~Åÿ÷|áÑùÙÀ{ZØ¼;âò&ÍzÍQ‚£€,q}ÿÕKØTùFéHŠe<KÕ!KÜaŸ4«™Dp²l->~,S	ÌËó•OpkZÃéW BÂ²×=ˆw<&"ME»¶ªÑ"Ô„#
T`A!ùÌVYÎ
Ìˆ(xÉ}†ì=kfat¥Y¼o p½~G­îÏV&mÚ%5ø¢¬eBş½:%–EÕ<8¹{³àŞ@òAv¥å iBÁauœqÁ$œ“	á&ãôN¥pzI†5`X¼.`PÍ¢ä·à \Ä—ë.¼C¥Q¥å˜¹(w„®@¡×ÉC §S&¡Šˆ½»vß#€G õˆ)ñûèé‡¾6[›õµBY³‹\ÎÄTªı½S*xµ–§j<Õ&Íéû5&zz%·e¤õ¸®ôŞö•³5f¥µ¢¸±!ù„6z÷àºVĞsÜËWóPÜ/`ß‹…U¯CÃÑØœôM4¼7<åWhb)(y¨‘.´‚ôÕâ1İµ¶±Ì]ÓˆH‹Âl²»;GOóœá¶
¨§œö/È ÇÂgó|…`”å¡xzÒ›„ô’e‹‚fœğ©k6* GÍq³kz¼Sa±K“¶™zÑ :4Ä-vmyîÜ’%×ÑZL7jØMÀ6áÙû lm‰ñ¹4¦ ë]¼ø©­UÀ>9Mòi˜…äˆ•}€S?Á!ìgGy…fñ'hqk¦çbY-oáí!\¡ÿ8=~öã«+„†ùç°ÓğØê´Ú†¹Î_·UUèƒ¨58Öv¬ekhÒì-ŒpK+É‹Ç´¯”ºl“
ğƒ' ¢ÂEcDL,#ˆ2S[š‹ƒŒŞç„íËbøŠ¦a<ÿø¬Eaûø Âv`K<®£ö³è]¤¸uo¶Ø³Ó’·>ş$QÉg£Ÿ<Ú<!È€¶²A0D jÖpi0æušP6€íŠ$Í9r8HVtCÀÃGPö<8P€Ğã˜<=’§ªî°![	ÊÍ"éÒLh-…cMÁÄÌGUhj
 ¥ ¹©Sx¼¹	ÃÉ$ŠmP^ÿïn>F—Qi°@ì–ß©)Ó´UÅ¹/Î__ˆË‘ @Ê©©¢Ù^ØMÃ+´´1Ú”_Ò\6ñIºQ§!ğØĞx Ï*{-i³
€ÂtÑÇKffğe2ïı Kâ.unm­Ò^Öùü•§À’M£›\rôXìJ1–üD+Í®ª2h9­®zè¶Ğ¸MĞÑŒ6Ìz<.ºø1ÿÚê>iíÕ¿rºOœ½^yw‘m³<îkOÖğq’JÛLÇ8•ëe“–+µ§0çh›®“<8åªgT„qÙ0Š"š‚vò>Áˆ8¥›a´€ô&Ñ"c,ÆSP9¼X†¬N,=³qöŞwâX˜¬©şVÎ`nİ!ç¤~+T>®œT×uÃBqbØ+h¥²V'z_Z¯¢Ï:’{TƒGKËØ‰±‚&jl*âV)îÛÂR q	´`,G’VsƒbozOJâsl¹†vÔ‡f©a…pøAÙÖïÆi‹ÇYD[Hùm¨ŞZn•Òâ¨Ç†rUşD$å¤\‹Kzz/Ö%Ëûp%ÍE%@ÓÁb6ëˆ{¬E¹R~az\'¸ŠËcò„ãÅ±KÿÕÎ-¦Bm–€U,ùWVÊkŞˆ	3J•\Şlâ[R¬bYƒLğ"øÀı,ûz–u¸¥¨#ş³OÚÂÑ6éCSmœú±´³d4ìD«XPPãì8À…G½*Veqj`£@sƒ,­ÑÒ¹w\h:Í×Ù¨éÀ>Ac3ıN‡Ey:§@`/5ú¹.s8Ø“‡«AjëZ\é*ŞçŒz•	t´Û8æÒ5RŞgp\¬²Æ5­6Õzƒ\ª™ZwÙÃsÈ2ë»Ö@Öİ¾Ğ]—[ SäÖ(UQ²î¦™ÎÖMW5Ñzê°"i¦šO9êÔœ=5Ãs¢`bAíÍÀÑìBMM#ÜPyĞ„•Æº:š®•¨jzzpó«ı:ÕWe?ìc`Ì£Áá1üûVCõæà@ò³Ä;y·)O¸ÿ.ğşî_ø4R…÷¿vŸ/‰>•ÂQšYjY·G[Jó¾Ü²2f-oè“»÷ıp±€7}9Ì Áµ„ª%†-ÏƒÃJ(3¶ÍÁä]gØ¢Ëüpj6/ÑÂßÅûK]6<íìXìï[ìW(M~oîğ½ç‚ÇÆïáC`ˆ°iÖë+^üC˜ÄlÍœX;aœÑI¬úEt;Íkºº‰!ÿğNbºI´C?÷şşCHÙU¿~A[H•Øg(şÕî˜Ü0•]ñŞ– ßô? ~sHìˆX½fes,ûAoÃß…È.¿Ô>]eP"!Œ`“·Ñ-§-¯F}ñ¨HRèÚô¯28‚¾]ó±ª–ZÏ£ºW ªì`M\¥4ÑQ¦ÇY€ïRØšB\äA d—ÊCòëĞ:ñÌÓ°ÉJã«2¨Œ/I®…Œ¯X9˜•ÎıSÚ(ıGÖtºRèŒúÌŸRä–jŞˆÜ²ÑA?ğ†Â2ÈÑ˜ÜœÆ@ö&ü§NSo6ardıJŸJgr½úÎ—ÙÔˆé0å¦ÕK?#Ï†*5V…¦Ì«´}Ş@T5®Ã'“qêı€¶ÁN»LYlÇ¢ı7ÑÄ±]eZšãÑ¾8Yu]KP9Pñ³ÈPvj¢ëHûh6IN…2rîKv½HWò‰ ^W†®°K–M™šNÃd˜8¤¤1zWíâ+ù„ô¼Á‹Ö|Aº= ©¢/Ùƒºv[Š…È‘ûâR¬jT¥óU‹2’Z÷UÂ§aX}yhn²­€ˆ£"Dù-sãhÃÎŞh™çél€¦²"0Ø4¥«NÕøågTòy¯Jì}QõM¿]ËÄ’±õ[Z¸MÜ•%ÏaVàÎÄó¨)‰°°HŸôÚGO'uŸ‘ë©<<rïm€9Ép¥D_x8ãpÂæëBÓjK¶š]¦&#=¶5#‡Ém>•Ê¡8	ïrzø§Òp‚v®¯öRÅ26Nãtùl!3K°·Äu`™§.£<Ca|¬²Jq+ÙÂ(U{òebøNT´`8´Ø/sŞ<â–òO¾#î¦-"`áû0‰W"'ÃÍBHöBĞpRS„pÀdÓîo-7d¦Èxr±-ô\y¾ÀVX°VãæÀÃ»¢£ĞÙÍR+9M‚3JÓ8’ªÔà‹?%/›Ä¥›¹öåy…=6}ïªŸÉræ§7şx,²Á×ÿ,õBtö581%²…ÅİsÉî%dºJ­%“«um@ıH­„twğDdÑ–™Û%ŒûíÅy»„æüC0›Çá‘ôŠ{Qò³˜vÆY§â.]Ä“–èv{èq÷<’mÃ0ù!ÊÆ $aºÌ¬·ÄÙy0™(RuæÂ2äı?\TÅ-8HòÊªò'F	[Yñ÷€r‚Eù¶áÏC°j+“(›ÇÁŠAê±C'/)¥ÈÂ8#Û÷óuš]cØö o#WÉà@vŠ]sN•F&äÂÿh¹·núgÙ4(¯ÁùÏWç¯¯pwRmß¼ÿâD0ûŒ+7[JoÑ2ƒo³Oø4Y"3Â§5_„ÕüõüÕË^ÂÔ©’Ãckø4qjŸfJzƒ?Ø³~BÿÓÅí>_ÚF¡—äé1 C˜)0ú„è3ÛFGğ ÔÖ÷5“]ùÚ¿XÛŞ‚D?I½ëb[[iW¿‚\¶ˆaYiŒéæmlq9SåÄPµí­I¹Ğ¤Ôï¶+dÅÑûğ.Â,±˜¸5ˆ³TÈˆŞl5¥˜=5’÷YYG½}sùòg+a{-uD>‡Ÿ1ü„´ö\œƒæª·5ÆZ¶ ]?1üÀšéıŞùa©óş€ŸµS)+|aÂP`rz›Dç‘ƒš‘‚&òsRäN4Ê¯p§ƒ¨)›Œµømp˜Ço
ûØhßaVRr^ÉMÆ$+CòJßâDÆjDşÜÍ*Ú
õ•£~›	Y3ñ¡Cš²»uÅ/2—2Èz”„Æ{˜téÛén¦¸|	^´¹A3ıØŠp3L®Qºc¬¶í:ßÌõA´§ŞVQ:Õ¯&¿Ó]o@å|Uóİ’ÚGÒw´!½à7Ë
²Hã­ç›Z–FËj0ãÕ’DŠ– öùvÆPºO\vhÕ´»¢hHÊÊŒúN…ù|ŞşŠô›
8­6ÈK'hx˜P<;áh{‡g&:»!ó¡~cÚnDò©-âS?­¿ÄcŒËIò@j†=àÛpAÛPL§ñy^©üÃÙÏUz1“h—Yšå äÎ÷#4[HÏ©ÉöéÑÏµ­ÃBkµn5—á«@äÑ,,óç
08í·y¿rŠóÒ±QjiyQWÀh—~,¯hÇ+¾¥I >C3¤¼'µ—šO&*0-L6([ÉÌ/„°ôÚ¶÷”°¢·W…qUŞí‹şÓ1?}ÕÉ‰øWZYë
Ká®¸„Ó†:P‚&à8Z²éë”åxcò*\¾|qïû;»xíû¡…ÀÊ‹5Ën±RoÒº)Í.ğŠ­.r1ë>¡}ølİ.áX'†.ı:dŞX.†íPZiTâ$œ=ÚP¢L7û™‘\‡0;6‘˜ş*^#¤t\^ıõüâB˜Køu‰­*i|¬T´NW
Iß.œî”“ºÎÇ×¼Æåğ¿ÖO*4§ÖG!³7àlü{4Ÿ‡w¿nÁ|0¹HG‘Š¶ºÈÜlæ—­Õµ°]æ?}Ñû´Èe“UÍ¢9kòµÂÕ²aK†6%
©ğ²â/xˆ‡Ûåà(Åiİ³(ø6ğ°AJ¬†µ#“=VKõª] ^–WB&g“¶¿…«“†lëk¢°^Şà&3ğ5:n
˜&wRû!W~ÍH”œú§åÁß«©ƒ~~J¥UŠT³IÂZË”7½¬a0éG¥*EÑÖŒ
0PÚlWœM&uXuŞSâ](¿€"O=>CcuÒÕ2eÂU[Ÿ7÷ú¦™l¨J·Ò+7!9ğj0|PÛË:d6 +Æâcã2™L36¿)¥ôg™w˜ô:F…c;¸A]® 2ZbçÉı>¼îõèa–½“e´:ªòYïW×f‹fgŞ\·;í'X K0‰;¨÷ígíq|Q71öú´j‘_¡z	ƒ»ùÑ3®”i[)]´0¯¹Ù¤We°Ò|éôŠû‡
 6À9«³‡–õÎ^dNÿ6Åïì”Óğ¸§EP¸dH®Q½©‘.ôµë|dœ´²±(‹¤Pq-R8‚èúû%·¸İµò#•z,Û%3°œÑ¸ûÑìÈ©™Ï÷‘©Ìœõ 4´ÃÇØÎTÚ!J²UØ‚š;–3IqÔT[NQBdGw¬lKP!+w¢ ø	v>¾¤Í”•¥à)¦ˆè;Rîê´ZıĞD®ìÈŒ;Yj7¦b½J;±šèË¨ÁD©Ïgxİßµ (\i¡èWBN5x]àã†˜À"LG>©+A‚‡àb6F\PôƒŞ$õêâsEã	õÊ‘µ%8 %£µTË#ŠˆX÷PyhÙİ‹ EìœlÓÀÌ?İ…½©†Êß¾Act‰Û?Š%?[ƒ—²‰¢‡/™©Xââú¶L\sûŞá £c##Æ@'²b\¶W~”¨;W~©:†^ìÊ˜*İhG¸­n«İ¶î]ê·íÁà°]Zá¥	aÜşh<‡…	ƒñpÿ0ÀúNq˜¤hÉÿ Õ{”fC7Ô~©Ôµ-İ²ú,6«o
ºÍÌëÛÚó1%·4¥ØİÃƒ¥Gâu
çiôÏâ
f•ÎÎYwleVÈ[×jA<UHœjk„Nˆ²Eb
y	ÒêóZ'ñVuk	ouN×)ÆË&RJaA%c)ZPS¯ş*fÍœâ©Ä>fíQ“ß®cÏ.û×ğN!Y¹Ø--(Iµh=éçstCwK©*_îPË‰~³î¼˜ˆ]ƒMD³I'L¦¡õñZG*“0ƒqŸ"\$“/=vD˜ÛöGS°öSªMºÏ¿Ú”§vq\ƒ”¨ªò5Ua½ú@µ2`•¿PN)Gvç© ï„ù]V/4¹iGmQº„HÿIÅR³íVk§SÅD¶:æéûï+›¬ïØ‚LCà ´¸9Ã6ºG`À†5]G` ÅÕ.£èLë:-a~0ê¸@(ï…3Š±­•ú]HÚ(.)´ hP•‰:HVğ?³è‹“TïŸÂè[<…ü9Ã8‘`n\–<Pr‰ñ7ËßmÌ4Úœ¿‰–ğ`+xÄå­Ì6’ËKÈËŒ«âwĞÆøYD}?‘ìñ˜¾!…åsÖbN2±[öÔ¢‡1y«º€W£uk Aø¼cZg»Œá2'Š§!FxC-!*z}¹š‘³ù^–ÆŞeË“ñê¡K¾vlºmv”ƒ~c8á^ÏhR «åv6&[5¬F0/aW;‰nÈÍšóılkÚOİ¬­¿-%]u0}G‚ÅzYšo1”»ˆ|êğex×›HinË‚ÇÅíºbXO†=‡âÑ8qÎZ”0ˆQòˆ}¶[Äc¶„^bÈuI1)@(hT1†\5l[6¦"«Fá8@­ß¥× N†Şc	Üï2·fĞ2ësş.€ÈÃ,o‰àïEçõl®Ş—_q7F¡–¥îI‚&ÁXr¿cš&-üwñ^¤Xˆ.ñvWÒ^&ÜõiÔ€%QÉ2	0O[‰¾ƒ6Uş<«Âõ´ËÌçÑ!1ÎVY!œ¼Ğól¹M‰‹%L‡¿İ”fyT™«r0\+}²NTGê`1 @±…g‚
¼•¼-ªey° ‹¨˜ë$6@ÚG!†`UIğ‘`©§q&UÂ9blA£^´É•¯õP”ë­²AÌÜ9S4Šâ(_¡L ‹Ê¬Á•ø¦³´´ĞpÒó8‡,<5TWúVßƒ»]M¯†‚>…†B;|“™F¹gX”>eõ*k›ë˜:è»/(Èa'Èmv0ŞöqYÛûÚÍ×k´Ú$©›¢Æu)káï+_W¸ê®£qO¢{-ß»g(EÈ¬8ÈÌX*W<‚ãÂ,[WİàGê†êµ=õl¥e-&GC/Hßİá§WäNÇHGq4Å8ŞMÓ8T×·½#•ÇiØ“p;ŠCE8ïì?ÈÀæši/´S­&	l{0´uU§´îÓ†^><5¬§\…³_«\õ]cSÍó½ì‰Êl°ÏGş®A‚eFP	2,º_v. •@R¦Ö·óFîq~ NÔu\ö×ç¤ $ä¼5Tùí†ùü>#Ì|ÏI'Œğ*ƒ´ôW«ä­~ããM',uç>©”®•Kòótîúj^ĞçWØè€k<~eûG¦•á•šVt~k4ujB•…Ï óõõµÍ*ÁƒÅOœaª¢µÑ&.áë¢W2w ÌˆÜÔ¸kTNYãÔÏU5 eZw`yŸø¤Ò3bRô;{ÁSéÊß…_e–ï.åöæÑ[]ü•Hî“6zÒ2ZD<Ò
`=Ê¤¨Pïz…z4Í÷ZÍáÖÁA}<ğÊKÜŒ¹„]Ln&.¬ t}làõ•‰ë@¹ôc;_a(O‘~½Öém‚ODQÅLÜnÛWùj©Ôğ'Ğe‡Âcƒq ¶ıÔ71d›wL³‰ÚbLÃ.İğÃäÀıÊÇÊ)Ç5¤z'N"€Gàè=z©sï ÇNRj/WÕ@-yø!ïP2#~!…İ(‚%‰Y«-Ú¬dYC±bwĞ-KıQq‘®‡>]®Æø¾¹v3ÒWN&‘LóöùqV«wç+ oE¹¹À®%@’Úh ÍL:ªºOøPkCMa„DÈa<ÀFÄ‰%6¥4$öAyãFßŸ]~?tßzÿ¦-Ã6: „Š4	ud×P½q.)ï1–4õS *÷²£&ù9«³:ûEmgÍÚ|z#¥6]kµY
+©Ùy3µö^¥ÔäÆ_¡ Šâ/µ’lV-»0°™ÁSS	éùÕù®èAm’Qã>[K.œ‚ZNÃÿ÷Ùïíjë
ñÖké“@-ïê;ëut%Èğ(”ì¼XÛÛbeÿA™AFÕM\QÃWc†/ó„Ib/¾¶­£Ëôİ¯Ş¯ÃÉõw89hÃÃõ“áNÂKÖ"88ŞwN‡|yÊF^×Mr(O¤Vi†ùÕÛëyN&ÚS\Æ!d)ÇB1(ÑEóDO#‰›Òõ:·»±Î`îNÌ]™2ñób‘ÃNíèf†" Å"Ó1^†µ‹EË¡‚—óVoªMjšÕŞ§t>È‰L>*ô\Løóóú´{:š•+S½)ï÷µ¤D˜±^· uÊ¡<zF‘L[$ıß²İÄŠ¤=%=&á°×«êŞS.GÊOSƒ8=aÏx­QB[4gUãıX_|ób—B¹I
ì{ön¯İ3ÅJâ-¾u˜Mò^'ıŠbi§{T«|Ë“Ùma¯]'Š¦”>)¯‡Í+‡Cë—¯b·UÓîßŞã¹ºÁdL)xƒüc¿Jh>X~ ºi“æ!ç‘™ªpÔ¼Ìq%®á§éHã×JgIDx#G‹tõÈAlskİÔ,’H,1©4v%àˆ’(ÊAâ¯bĞç±Cİƒg*œëƒoÔ§€æôMTÁ¸ù)ÉyÁ²+Í†AEp{¦Š`°—aĞ!ÜrS*ó!æ2r2ó¡ÌfŞ=(èÌ–7Z+Qé@¾5›´:Pb›E}‘â>°]‰ÏÚu³jmˆd¿5(ŠH€`­e=CK¸^ôŒÜeÙõÒ}òLĞ'»•ªlœõìº½"ZÊ;ÀaAªuY>P r"©µĞì‚„ ¹W\©EVh{E«Ô¢@-súêı²8pÀö}éHU¹økï¯+ñ[éšÇ‚µh.“gÉ¸+ÃçUÈy9åf%‡hM’P×9w8éw}üNG8Ï7Œ7ÀæİiˆÄ¼?ÛF›šŒ7$› ~ßmøcÀ‡¬¬´¥‘ÓƒÍ7ıâ JÇ¯`ŒÖ‘ÃÃ£~pôÏ££qxô—;÷å§«NÃ|z ¿õ²Q”ô¢™0•Š-üVÀU(çşV|u#ÌnÎ¼çp¼¾º>jßÿÓÚ.Âh>¬(4H\ûYÿH^ïø/PK    -KØPD¨~!'       script/main.pl]]KÃ0†ïó+eĞìÄÛeTf-z!BÈÒ3h“,M÷aí7íÄßË“œ‡ç=Dî–¸—
K˜eù[_¤–Ûd¯Ûlˆ 'àsâVAQtïjôƒ„EîüŞIºJw\…Ç»vú¢“ZÍa‹¡A[—Z€1áüC‹‰Šgéè]´ !¤¹ÀìK¸‡™wH’µÜ­¸¨°ÿÓ*6/yúœ|CjE%˜$ïÒÄK…'ÊØÃÓ:cÌGXƒÍ­çá‘×ĞOôxyæ¼Á’†­°Ò¸ÛO‹¦›:Œ`˜ÄÆh¥D8hÃ¥ò¯	¬¸
ï…
Æ>p]‡à&H ×Ğv¢‚½1’,
§íè¨yEŸ©'³bW+úë|ãQÈPK     -KØP                      íAO^  lib/PK     -KØP                      íAq^  script/PK    -KØP×LQµ;  Ã             ¤–^  MANIFESTPK    -KØP#“/S¡   Ù              ¤÷_  META.ymlPK    -KØP‚L"å  za             ¤¾`  lib/YAML/Tiny.pmPK    -KØPådáĞa'                ¤Ñ|  script/greps.plPK    -KØPD¨~!'               ¤_¤  script/main.plPK      Š  ²¥    f79ec9d03cf2463ecfefb5ab29654f23d62f409a CACHE  I1
PAR.pm
